const role=document.getElementById("role"),create=document.getElementById("create");create.onclick=async()=>{alert("Secure server endpoint required. The frontend never creates privileged Auth users directly.");};document.getElementById("approve").onclick=()=>alert("Only an authenticated Super Admin can approve this request. The server should issue a one-time temporary credential and set force_password_change=true.");