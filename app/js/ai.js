const form=document.getElementById("ask"),input=document.getElementById("prompt"),messages=document.getElementById("messages");
function bubble(text,who){const d=document.createElement("div");d.className="bubble "+who;d.textContent=text;messages.appendChild(d);messages.scrollTop=messages.scrollHeight}
form.onsubmit=async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;bubble(text,"user");input.value="";const wait=document.createElement("div");wait.className="bubble ai";wait.textContent="Thinking…";messages.appendChild(wait);
try{const r=await fetch("/api/ai-assistant",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text})});if(!r.ok)throw new Error("AI service unavailable");const data=await r.json();wait.textContent=data.answer||"I couldn't generate an answer right now."}catch(err){wait.textContent="AI service is not connected yet. Please use the Help/How to Use section or contact the Head Admin on WhatsApp."}};
